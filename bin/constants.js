"use strict";

const PERIOD = ".",
      DEFAULT = "default",
      EMPTY_STRING = "",
      DOUBLE_SPACE = "  ",
      HIGHMARK_CLI = "Highmark-CLI";

module.exports = {
  PERIOD,
  DEFAULT,
  EMPTY_STRING,
  DOUBLE_SPACE,
  HIGHMARK_CLI
};
