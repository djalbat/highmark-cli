"use strict";

const HELP_COMMAND = "help",
      SERVER_COMMAND = "server",
      VERSION_COMMAND = "version",
      PUBLISH_COMMAND = 'publish';

module.exports = {
  HELP_COMMAND,
  SERVER_COMMAND,
  VERSION_COMMAND,
  PUBLISH_COMMAND
};
