"use strict";

const PERIOD = ".";
const DEFAULT = "default";
const DOUBLE_SPACE = "  ";
const HIGHMARK_CLI = "Highmark-CLI";

module.exports = {
  PERIOD,
  DEFAULT,
  DOUBLE_SPACE,
  HIGHMARK_CLI
};
